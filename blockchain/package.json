{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "hardhat": "^2.10.1"
  },
  "scripts": {
    "deploy": "npx hardhat run --network localhost scripts/deploy.ts",
    "node": "npx harhat node",
    "test": "npx hardhat test"
  }
}
